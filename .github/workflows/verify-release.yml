name: Verify Release

on: 
  workflow_dispatch:
  push:
    branches:
      - release

jobs:
  call-verify-release:
    uses: lacework/oss-actions/.github/workflows/tf-verify.yml@main
    secrets: inherit
